# database
android package for sqlite databse support (kotlin)

has interfaces to define table declarations,
has interfaces and classes to pars sqlite-commands,
has abstract table classes for base sqlite functions.
